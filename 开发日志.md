# Librarian — 开发日志

> 记录从 0 到 1 构建这个学术文献管理工具的完整过程，包含每个功能的设计思路、遇到的问题和解决方案。

---

## 起点：一个研究者的痛点

**2026年5月** — 出发点是法学/社会科学研究者在写论文时的真实痛苦：

> PDF 文献散落在硬盘各处，划线笔记无法检索，写综述时要在十几个文件之间反复跳转，AI 对话记不住上下文……

目标是构建一个真正**以文献为中心**的工作台：导入 → 阅读划线 → AI 分析 → 生成综述，流程一气呵成。

---

## 功能迭代记录

### v0.1 — 基础框架

**第一个问题：** 选 Streamlit 还是自建前端？

**决策：** 选 Streamlit。理由：
- 用户是研究者本人，部署在本地
- Python 生态可以直接调用 LangChain / fitz / ChromaDB
- 不需要前后端分离的工程复杂度

**基础功能：**
- SQLite 数据库（`database.py`）管理论文、划线、文件夹、My Thesis 等数据
- `save_paper()` 支持手动填写元数据，或从 PDF 自动解析
- 文献列表、删除

---

### v0.2 — PDF 解析与元数据提取

**核心挑战：** PDF 结构千差万别，中英文论文版式完全不同。

**方案：** 用 PyMuPDF（fitz）`rawdict` 模式逐 span 分析字号、位置：

```python
# 标题：找第一页字号最大的文本行
blocks = doc[0].get_text("dict")["blocks"]
candidates = [(max_size, line_text), ...]
candidates.sort(key=lambda x: -x[0])
```

**遇到的问题：**
- 期刊名藏在页眉，字号小且位置不固定 → 加了"水平中心距离"排序，越靠近页面水平中央越可能是期刊名
- 中文作者行经常被机构地址污染 → 建了 `_inst_words` 黑名单过滤机构词
- 年份有时出现在多处 → 优先找 "published/received/©" 附近的年份，次选出现频率最高的

**CrossRef 补全：**  
发现 DOI 唾手可得（PDF 前3页几乎必有），通过 `https://api.crossref.org/works/{doi}` 可以拿到结构化的元数据，于是加了 DOI 自动查询作为兜底。

---

### v0.3 — PDF 阅读器 + 划线

**需求：** 在 Streamlit 里做一个「能划线」的 PDF 阅读器。

**这是整个项目最难的部分。**

Streamlit 没有原生 PDF 渲染，也不支持复杂的文字选取交互。

**方案：** 自定义 Streamlit 组件（`_pdf_component/index.html`）：
1. Python 用 fitz 将 PDF 页面渲染为 PNG base64
2. 通过 `streamlit.components.v1` 传给 HTML 组件
3. 在 HTML 里叠加 text-layer（透明 div 网格，每个 span 一个）
4. 用鼠标拖拽 JavaScript 实现"框选高亮"
5. 选中后通过 `window.parent.postMessage` 将划线文本传回 Python

**重复触发 bug：**  
组件回调每次 rerun 都会重复触发已处理的划线。  
→ 解决：用 `last_hl_{pid}` 记录上次处理的划线内容，相同内容不重复处理；处理后清除 session_state 里的组件值。

**划线面板** 同样是独立组件（`_hl_component/index.html`），支持点击跳转到对应页码。

---

### v0.4 — Agent 集成

**架构设计：**

```
LiteratureAssistant
├── global_agent      → 全库分析（list_all_papers / search_paper_content）
└── paper_agents[id]  → 单篇论文上下文 Agent（懒加载，按需创建）
```

用 LangGraph `create_react_agent` + `MemorySaver`，多轮对话状态自动维护。

**工具集（`tools.py`）：**
- `get_paper_details` — 获取论文元数据
- `list_all_papers` — 列出全库
- `search_paper_content` — 全文语义检索（ChromaDB）
- `get_highlights` — 获取划线
- `generate_citation` — 生成 APA/MLA/BibTeX
- `save_paper_notes` — 保存笔记

**My Thesis 上下文：**  
在侧边栏让用户填写自己的论文标题和核心论点，每次 Agent 调用时自动注入 system prompt，使分析始终"以你的研究为中心"。

---

### v0.5 — UI 大改版：从多页面到单页面 Tab

**用户反馈：** 侧边栏导航"没有逻辑条理"，各功能模块堆在一起，统计数字放错地方。

**重构方案：** 主内容改为 `st.tabs`（Library + Agent），Import 改为 `st.dialog` 弹窗，Search 合并入 Library 工具栏。

**主要改动：**

```
之前                           之后
────────────────────           ────────────────────
侧边栏：Library/Import/        侧边栏：My Thesis + AI Engine
       Agent/Search            
主内容：单页面视图              主内容：Library Tab + Agent Tab
                               Library 内：Import 按钮 → dialog
                               Library 内：搜索框（Title/Semantic）
```

---

### v0.6 — Library 视图重构：文件夹优先

**用户反馈：**
> "library 点进来应该是文件夹，文件夹里面是 paper，global agent 也作为一个模块展示在文件夹里"

**实现：** 引入 `st.session_state["lib_view"]` 管理三层视图：

| lib_view 值 | 显示内容 |
|-------------|---------|
| `"home"` | 文件夹网格卡片（All Papers + 各 folder） |
| `"folder_{id}"` | 该文件夹内的论文 + Tag 筛选 + Folder Agent |
| `"all"` | 全库论文 + Tag 筛选 |

文件夹主页是卡片网格（3列），每张卡片显示图标、名称、论文数量，点击 Open 进入。

---

### v0.7 — Tag Pill 样式问题

**遇到的问题：**  
`st.button` 配合 CSS 类 `.pill-btn > button` 的方案完全不起作用——Streamlit 渲染时 `st.markdown("<div class='pill-btn'>")` 和 `st.button()` 是平行 DOM 节点，无法形成父子关系，CSS 选择器永远匹配不到。

**解决方案：** 升级到 `st.pills`（Streamlit 1.57 新增原生组件），支持 `selection_mode="multi"`，样式通过 `[data-testid="stPills"] button` 覆盖。

```python
selected = st.pills("Filter", keywords, selection_mode="multi", key="tag_pills")
```

效果：原生 pill 多选，点击即选中/取消，选中态深色背景，完全符合预期。

---

### v0.8 — Folder Agent：关联特定文件夹的文献

**需求：**
> "global agent 是在具体的 folder 里面，其关联的文献就是该 folder 里的"

**实现：** 每个 folder 视图底部附加一个 Agent 区块，在 system prompt 里注入该 folder 的论文列表：

```python
_folder_ctx = f"Focus ONLY on the {len(_fp_list)} papers in folder '{_cur_coll['name']}': {', '.join(_fp_titles[:10])}"
```

每个 folder 有独立的 `_folder_chat_{fid}` 和 `_folder_thread_{fid}`，多 folder 互不干扰。

---

### v0.9 — Paper Card 交互优化

**需求：**
> "这个 collection 去掉（expander），加一个移动按钮可以选择移到哪个 folder"

**改动：**
- 移除 `st.expander("📁 Collections")` —— 展开器打断视觉流，且功能不直观
- 卡片右上角加 **⊞** 图标按钮，点击触发 `@st.dialog` 弹窗
- 弹窗列出所有 folder，已加入的显示 ✓ + Remove，未加入的显示 + Add
- 卡片本体仍保留 `📁 folderName` 小标签作为状态提示

---

## 遇到的主要技术问题汇总

| 问题 | 根因 | 解决方案 |
|------|------|---------|
| CSS 类无法作用于 `st.button` | Streamlit 不允许 HTML 包裹 Python widget | 改用 `st.pills` 原生组件 |
| PDF 划线重复触发 | 组件值在每次 rerun 时重新读取 | `last_hl_{pid}` 去重 + rerun 后清除组件值 |
| `st.tabs` 无法命令式切换 | Streamlit 无此 API | 用 `_pending_tab` session_state 标记，下次 rerun 时读取（视觉上有延迟，可接受） |
| 中文 PDF 作者行解析噪声 | 机构地址紧跟作者名 | 建黑名单词典过滤机构词汇 |
| `st.pills default` 每次 rerun 重置 | `default` 参数是初始值，重渲染时仍生效 | 移除 `default` 参数，直接从 `st.pills` 返回值读取当前选中状态 |
| git push 权限被拒 | remote 指向 datawhalechina 组织仓库 | 改为推送到个人仓库 `freshrainny/literature-agent` |

---

## 架构总结

```
用户浏览器（Streamlit）
├── app.py                    — 主入口，UI 渲染 & 交互逻辑
│   ├── 侧边栏：My Thesis + AI Engine
│   ├── Library Tab
│   │   ├── home 视图（文件夹网格）
│   │   ├── folder_{id} 视图（论文 + Tag + Folder Agent）
│   │   └── all 视图（全库 + Tag 筛选）
│   └── Agent Tab（Global Agent）
│
├── agent.py                  — LangGraph ReAct Agent
│   ├── LiteratureAssistant
│   │   ├── global_agent（全库）
│   │   └── paper_agents[id]（单篇，懒加载）
│   └── LLM 工厂（9 种 provider）
│
├── database.py               — SQLite（论文/划线/文件夹/My Thesis）
├── tools.py                  — Agent 工具集
├── pdf_viewer.py             — PDF 组件 Python 侧
├── _pdf_component/index.html — PDF 渲染 + 划线交互
└── _hl_component/index.html  — 划线面板
```

**数据流：**
```
PDF 上传 → fitz 解析元数据 → SQLite 存储
          → fitz 提取全文 → ChromaDB 向量索引
                               ↓
                    Agent 语义检索 → LLM 分析
                               ↓
                    笔记 / 综述草稿 → SQLite 保存
```

---

## 设计决策备忘

**为什么用 SQLite 而不是 JSON 文件？**  
Collection 关系（论文 ↔ 文件夹 多对多）用 JSON 很难查询，SQLite 天然支持关联查询，且零配置。

**为什么不用 st.session_state 缓存全部数据？**  
Streamlit 每次交互都会重新执行整个脚本，session_state 存大对象（论文列表）会导致内存持续增长。保持"每次从 DB 查"更稳定。

**为什么 Folder Agent 用 global_chat 而不是独立 Agent？**  
单独创建 Agent 需要维护更多 session_state，且 folder 的文献范围是动态的。通过在 system prompt 前注入文件夹上下文，复用 global_agent 的工具集，更灵活，代价仅是多一小段 context 文字。

---

*由 AI 辅助开发，2026年5月*
