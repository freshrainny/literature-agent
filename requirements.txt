# 核心依赖
streamlit>=1.32.0          # Web UI 框架
langchain>=0.2.0            # LangChain 核心
langchain-openai>=0.1.0     # OpenAI 集成
langgraph>=0.1.0            # Agent 框架（ReAct）
langchain-core>=0.2.0       # LangChain 核心组件

# PDF 处理
pymupdf>=1.23.0             # PDF 解析（fitz），你已经在用了

# 网络请求（获取文献元数据）
requests>=2.31.0

# 向量数据库（语义搜索，可选）
chromadb>=0.5.0

# 工具库
python-dotenv>=1.0.0        # 管理 .env 环境变量
